name: test-pipeline
on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  test:
    - runs-on: ubuntu-latest
    - uses: action/checkout@v2
    - name: test
    - run: go test ./...